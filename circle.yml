machine:
  services:
    - docker

dependencies:
  override:
    - echo "skipping pip install"
checkout:
  post:
    - git clone https://github.com/somaticio/somaticagent.git

test:
  override:
    - tests/test_api.sh
